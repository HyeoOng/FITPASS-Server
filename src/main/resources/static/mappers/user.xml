<?xml  version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ssafy.fitpass.model.dao.UserDao">

    <insert id="insertUser" parameterType="User">
        INSERT INTO user (email, password, name, nn, admin, reg_date, profile)
        VALUES (#{email}, #{password}, #{name}, #{nn}, 0, now(), #{profile})
    </insert>

    <!--login method-->
    <select id="selectOne" parameterType="Map" resultType="RetUser">
        SELECT user_id, email, name, nn, admin, reg_date, profile
        FROM user
        WHERE email=#{email} AND password=#{password}
    </select>

    <select id="selectOne" parameterType="int" resultType="RetUser">
        SELECT user_id, email, name, nn, admin, reg_date, profile
        FROM user WHERE user_id=#{userId}
    </select>

    <select id="selectAll" resultType="RetUser">
        SELECT user_id, email, name, nn, admin, reg_date, profile
        FROM user
    </select>

    <update id="updateUser" parameterType="User">
        UPDATE user
        SET email=#{email}, name=#{name}, nn=#{nn}, profile=#{profile}
        WHERE user_id=#{userId}
    </update>

    <delete id="deleteUser" parameterType="int">
        DELETE FROM user WHERE user_id=#{userId}
    </delete>

    <select id="checkEmail" parameterType="string">
        SELECT * FROM user WHERE email=#{email}
    </select>

    <select id="checkNn" parameterType="String">
        SELECT * FROM user WHERE nn=#{nn}
    </select>

    <!--admin-->
    <select id="selectAllAdmin" resultType="RetUser">
        SELECT user_id, email, name, nn, admin, reg_date, profile
        FROM user
        WHERE admin IN (1, 2)
    </select>

    <update id="updateAdmin" parameterType="int">
        UPDATE user SET admin=1 WHERE user_id=#{userId}
    </update>

</mapper>